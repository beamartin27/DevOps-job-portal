# 🏗️ Architecture Overview - Job Portal

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                      (Browser / Mobile)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REACT FRONTEND                              │
│                    (Port 3000 - Client)                          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Header     │  │  SearchBar   │  │  JobBoard    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   JobCard    │  │  JobDetail   │  │  Pagination  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐                            │
│  │   Loading    │  │ ErrorMessage │                            │
│  └──────────────┘  └──────────────┘                            │
│                                                                   │
│  ┌───────────────────────────────────────────┐                  │
│  │         Services Layer (API Client)        │                  │
│  │           - Axios Configuration            │                  │
│  │           - React Query Setup              │                  │
│  └───────────────────────────────────────────┘                  │
│                                                                   │
│  ┌───────────────────────────────────────────┐                  │
│  │            Utils Layer                     │                  │
│  │    - Date Formatters                       │                  │
│  │    - Salary Formatters                     │                  │
│  │    - Location Helpers                      │                  │
│  └───────────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ API Calls
                              │ (Axios + React Query)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   NODE.JS BACKEND                                │
│                  (Port 5000 - Server)                            │
│                                                                   │
│  ┌───────────────────────────────────────────┐                  │
│  │         Express.js Application            │                  │
│  │                                            │                  │
│  │  Routes:                                   │                  │
│  │    GET  /api/jobs           ─────────┐    │                  │
│  │    GET  /api/jobs/:id       ─────┐   │    │                  │
│  │    GET  /api/health         ──┐  │   │    │                  │
│  └────────────────────────────────│──│───│────┘                  │
│                                   │  │   │                       │
│  ┌────────────────────────────────│──│───│────┐                  │
│  │      Middleware Stack         │  │   │    │                  │
│  │    - CORS                     │  │   │    │                  │
│  │    - JSON Parser              │  │   │    │                  │
│  │    - Error Handler            │  │   │    │                  │
│  └────────────────────────────────│──│───│────┘                  │
│                                   │  │   │                       │
│  ┌────────────────────────────────│──│───│────┐                  │
│  │      Business Logic           │  │   │    │                  │
│  │    - Job Filtering ◄──────────┼──┼───┘    │                  │
│  │    - Search Logic  ◄──────────┼──┘        │                  │
│  │    - Pagination    ◄──────────┘           │                  │
│  └───────────────────────────────────────────┘                  │
│                                                                   │
│  ┌───────────────────────────────────────────┐                  │
│  │          Mock Data Layer                   │                  │
│  │    (Ready for Real API Integration)        │                  │
│  │                                            │                  │
│  │  - 10 Sample Jobs                          │                  │
│  │  - RapidAPI Structure                      │                  │
│  │  - Can be replaced with DB/API             │                  │
│  └───────────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Component Hierarchy

```
App (Root)
└── QueryClientProvider
    └── JobBoard
        ├── Header
        │   └── Logo + Navigation
        │
        ├── SearchBar
        │   ├── Search Input
        │   ├── Location Input
        │   ├── Remote Filter
        │   └── Action Buttons
        │
        ├── JobCard (Multiple)
        │   ├── Company Logo
        │   ├── Job Title
        │   ├── Organization
        │   ├── Location Badge
        │   ├── Employment Type
        │   ├── Salary Info
        │   └── Posted Date
        │
        ├── JobDetail (Conditional)
        │   ├── Close Button
        │   ├── Company Logo
        │   ├── Job Title
        │   ├── Meta Information
        │   ├── Description
        │   └── Apply Button
        │
        ├── Pagination
        │   ├── Previous Button
        │   ├── Page Numbers
        │   └── Next Button
        │
        ├── Loading (Conditional)
        │   └── Spinner + Text
        │
        └── ErrorMessage (Conditional)
            └── Alert + Message
```

---

## Data Flow Diagram

```
USER ACTION
    │
    ▼
┌─────────────────┐
│ User enters     │
│ search term     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ SearchBar       │
│ component       │
│ captures input  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Form Submit     │
│ Handler         │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Update filters  │
│ in state        │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ React Query     │
│ detects change  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ fetchJobs()     │
│ API call        │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Axios sends     │
│ HTTP GET        │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Express Server  │
│ receives req    │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Filter logic    │
│ processes query │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Mock data       │
│ filtered        │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ JSON Response   │
│ sent back       │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ React Query     │
│ caches data     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Components      │
│ re-render       │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ UI Updates      │
│ with results    │
└─────────────────┘
    │
    ▼
USER SEES RESULTS
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────┐
│                  STATE LAYERS                        │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────────────────────────────┐           │
│  │   SERVER STATE (React Query)         │           │
│  │                                       │           │
│  │   - Job listings                      │           │
│  │   - Individual job details            │           │
│  │   - Pagination data                   │           │
│  │   - Loading status                    │           │
│  │   - Error status                      │           │
│  │   - Cached responses                  │           │
│  └─────────────────────────────────────┘           │
│                                                      │
│  ┌─────────────────────────────────────┐           │
│  │   COMPONENT STATE (useState)         │           │
│  │                                       │           │
│  │   - Selected job (JobBoard)           │           │
│  │   - Search filters (JobBoard)         │           │
│  │   - Form inputs (SearchBar)           │           │
│  │   - Current page (Pagination)         │           │
│  └─────────────────────────────────────┘           │
│                                                      │
│  ┌─────────────────────────────────────┐           │
│  │   UI STATE (Component-local)         │           │
│  │                                       │           │
│  │   - Hover states                      │           │
│  │   - Focus states                      │           │
│  │   - Modal visibility                  │           │
│  │   - Animation states                  │           │
│  └─────────────────────────────────────┘           │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## API Request Flow

```
Frontend                    Backend
   │                           │
   │   GET /api/jobs?search=x  │
   ├───────────────────────────>
   │                           │
   │                    ┌──────┴──────┐
   │                    │ Parse query │
   │                    │ parameters  │
   │                    └──────┬──────┘
   │                           │
   │                    ┌──────┴──────┐
   │                    │   Filter    │
   │                    │  mock data  │
   │                    └──────┬──────┘
   │                           │
   │                    ┌──────┴──────┐
   │                    │   Apply     │
   │                    │ pagination  │
   │                    └──────┬──────┘
   │                           │
   │      JSON Response        │
   <───────────────────────────┤
   │                           │
   │  { success: true,         │
   │    data: [...],           │
   │    pagination: {...} }    │
   │                           │
┌──┴──┐                        │
│Cache│                        │
│Data │                        │
└──┬──┘                        │
   │                           │
┌──┴────┐                      │
│Display│                      │
│Results│                      │
└───────┘                      │
```

---

## File Dependencies

```
App.js
  ├─ imports: React, QueryClient, QueryClientProvider
  ├─ imports: JobBoard component
  └─ imports: App.css
      │
      └─> JobBoard.jsx
            ├─ imports: React, useState
            ├─ imports: useQuery from React Query
            ├─ imports: Header, SearchBar, JobCard, JobDetail, Pagination
            ├─ imports: Loading, ErrorMessage
            ├─ imports: fetchJobs from services/api
            └─ imports: JobBoard.css
                │
                ├─> Header.jsx
                │     ├─ imports: React
                │     ├─ imports: Briefcase from lucide-react
                │     └─ imports: Header.css
                │
                ├─> SearchBar.jsx
                │     ├─ imports: React, useState
                │     ├─ imports: Search, MapPin, Filter from lucide-react
                │     └─ imports: SearchBar.css
                │
                ├─> JobCard.jsx
                │     ├─ imports: React
                │     ├─ imports: icons from lucide-react
                │     ├─ imports: formatters from utils
                │     └─ imports: JobCard.css
                │
                ├─> JobDetail.jsx
                │     ├─ imports: React
                │     ├─ imports: icons from lucide-react
                │     ├─ imports: formatters from utils
                │     └─ imports: JobDetail.css
                │
                ├─> Pagination.jsx
                │     ├─ imports: React
                │     ├─ imports: ChevronLeft, ChevronRight
                │     └─ imports: Pagination.css
                │
                ├─> Loading.jsx
                │     ├─ imports: React
                │     └─ imports: Loading.css
                │
                └─> ErrorMessage.jsx
                      ├─ imports: React
                      ├─ imports: AlertCircle
                      └─ imports: ErrorMessage.css

services/api.js
  ├─ imports: axios
  ├─ exports: fetchJobs()
  └─ exports: fetchJobById()

utils/formatters.js
  ├─ exports: formatDate()
  ├─ exports: formatSalary()
  ├─ exports: getLocationText()
  └─ exports: truncateText()
```

---

## Routing (Current Implementation)

```
Currently Single Page Application (SPA)

http://localhost:3000/
    │
    └─> JobBoard (Main View)
         ├─ Job List (Left/Top)
         └─ Job Detail (Right/Bottom - Conditional)

Future routing could be:
/                  → Home / Job Listings
/jobs/:id          → Individual Job Page
/search            → Advanced Search
/saved             → Saved Jobs
/profile           → User Profile
/login             → Login Page
/signup            → Signup Page
```

---

## CSS Architecture

```
Global Styles (App.css)
  │
  ├─ CSS Variables (Design System)
  │   ├─ Colors (primary, secondary, text, borders)
  │   ├─ Spacing (xs, sm, md, lg, xl)
  │   ├─ Typography (font sizes)
  │   ├─ Border Radius
  │   └─ Transitions
  │
  └─ Base Styles
      ├─ * (reset)
      ├─ body
      └─ .App

Component Styles (Component.css)
  │
  └─ BEM-like naming
      ├─ .component
      ├─ .component__element
      ├─ .component__element--modifier
      └─ Media Queries
          ├─ @media (max-width: 640px)
          ├─ @media (min-width: 641px) and (max-width: 1024px)
          └─ @media (min-width: 1025px)
```

---

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────────┐
│              USERS / BROWSERS                    │
└─────────────────────────────────────────────────┘
                      │
                      │ HTTPS
                      ▼
┌─────────────────────────────────────────────────┐
│              CDN (Cloudflare, etc.)              │
│          (Caches static assets)                  │
└─────────────────────────────────────────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
         ▼                         ▼
┌────────────────┐       ┌────────────────┐
│  Frontend      │       │   Backend      │
│  (Vercel/      │       │   (Heroku/     │
│   Netlify)     │       │    Railway)    │
│                │       │                │
│  React Build   │       │  Express API   │
│  Static Files  │       │  Node.js       │
└────────────────┘       └────────────────┘
                                 │
                                 ▼
                         ┌────────────────┐
                         │   Database     │
                         │   (MongoDB/    │
                         │   PostgreSQL)  │
                         └────────────────┘
                                 │
                                 ▼
                         ┌────────────────┐
                         │  External API  │
                         │  (RapidAPI)    │
                         └────────────────┘
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────┐
│              SECURITY LAYERS                     │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │     Environment Variables           │        │
│  │  - API keys in .env files           │        │
│  │  - Not committed to git             │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │     CORS Configuration              │        │
│  │  - Enabled on backend               │        │
│  │  - Allows frontend requests         │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │     Input Validation                │        │
│  │  - Query parameter sanitization     │        │
│  │  - SQL injection prevention         │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │     HTTPS (Production)              │        │
│  │  - SSL/TLS certificates             │        │
│  │  - Encrypted data transmission      │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │     Future: Authentication          │        │
│  │  - JWT tokens                       │        │
│  │  - Password hashing (bcrypt)        │        │
│  │  - Session management               │        │
│  └────────────────────────────────────┘        │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## Performance Architecture

```
┌─────────────────────────────────────────────────┐
│         PERFORMANCE OPTIMIZATIONS                │
├─────────────────────────────────────────────────┤
│                                                  │
│  Frontend:                                       │
│  ┌────────────────────────────────────┐        │
│  │  React Query Caching                │        │
│  │  - 5 minute stale time              │        │
│  │  - Background refetching            │        │
│  │  - Automatic garbage collection     │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │  Component Optimization             │        │
│  │  - Minimal re-renders               │        │
│  │  - useCallback for handlers         │        │
│  │  - React.memo (ready to use)        │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  ┌────────────────────────────────────┐        │
│  │  CSS Performance                    │        │
│  │  - GPU-accelerated animations       │        │
│  │  - CSS variables                    │        │
│  │  - Optimized selectors              │        │
│  └────────────────────────────────────┘        │
│                                                  │
│  Backend:                                        │
│  ┌────────────────────────────────────┐        │
│  │  Efficient Data Structures          │        │
│  │  - Array filtering                  │        │
│  │  - Slice for pagination             │        │
│  │  - Minimal processing               │        │
│  └────────────────────────────────────┘        │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
Error Occurs
    │
    ▼
┌─────────────────┐
│ API Request     │
│ Fails           │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Axios catches   │
│ error           │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ React Query     │
│ error state     │
│ set to true     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Component       │
│ renders error   │
│ message         │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ ErrorMessage    │
│ component       │
│ displays        │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ User sees       │
│ friendly error  │
│ + retry option  │
└─────────────────┘
```

---

## Build Process

```
Development:
npm run dev
    │
    ├─> Backend: nodemon server/index.js (hot reload)
    └─> Frontend: react-scripts start (port 3000)

Production:
npm run build
    │
    └─> Frontend: 
            │
            ├─ Transpile JSX to JS
            ├─ Bundle with Webpack
            ├─ Minify JS/CSS
            ├─ Optimize images
            ├─ Generate source maps
            └─ Output to build/
                │
                └─> Deployable static files
```

---

This architecture documentation provides a comprehensive overview of how all parts of the system work together!

